var a = {};

a.add = 'this is add in branch iss53';
a.finished = 'this is add in branch iss53 finished';

var b = {};

b.add = 'this is add in branch hotfix';

var fn = function( arg ){
	//
};

init( xxx );